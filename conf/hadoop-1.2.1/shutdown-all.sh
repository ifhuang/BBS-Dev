#!/bin/bash

ssh root@hadoop-4 shutdown now
ssh root@hadoop-5 shutdown now
ssh root@hadoop-6 shutdown now
ssh root@hadoop-7 shutdown now
ssh root@hadoop-9 shutdown now
ssh root@hadoop-10 shutdown now
ssh root@hadoop-11 shutdown now
ssh root@hadoop-12 shutdown now
ssh root@hadoop-13 shutdown now
ssh root@hadoop-14 shutdown now
ssh root@hadoop-15 shutdown now
ssh root@hadoop-16 shutdown now
ssh root@hadoop-19 shutdown now
ssh root@hadoop-20 shutdown now
ssh root@hadoop-21 shutdown now
ssh root@hadoop-22 shutdown now
ssh root@hadoop-23 shutdown now
ssh root@hadoop-24 shutdown now
ssh root@hadoop-25 shutdown now
ssh root@hadoop-26 shutdown now
ssh root@hadoop-27 shutdown now
ssh root@hadoop-28 shutdown now
ssh root@hadoop-29 shutdown now
ssh root@hadoop-32 shutdown now